# RESULT — demo_sudoku_vs_essay (the RUN is the judge)

*Generated by run_demo.py AFTER PREDICTION.md was committed.*

## Domain A — sudoku-solution-validity
- router verdict: **SEARCH-AND-VALIDATE** (kappa_hint=None)
- pipeline outcome: **KAPPA>0_VERIFIER_FOUND** (kappa=1.0)
- per-candidate run results:
    1. REJECTED (HALLUCINATED_NONRUNNING)
    2. REJECTED (SOUNDNESS_INCOMPLETE_MISSED_VIOLATION_TYPE)
    3. VALIDATED (kappa=1, exercised=['box_duplicate', 'column_duplicate', 'row_duplicate'])
- registration record: `BOOTSTRAPPED_SUDOKU-SOLUTION-VALIDITY` (violation types exercised: ['box_duplicate', 'column_duplicate', 'row_duplicate'])
- ceiling carried: Toy-passing != frontier-correct. This verifier passed its KNOWN-ANSWER cases; th...

- **Domain A prediction held: True** (route=True, outcome=True, cand1-hallucinated-rejected=True, cand2-incomplete-rejected=True, cand3-validated=True)

## Domain B — essay-persuasiveness-quality
- router verdict: **ARMOR-ONLY** (kappa_hint=0.0)
- pipeline outcome: **KAPPA=0_ARMOR_ONLY** (kappa=0.0)
- why: No exact verifier found/validated in 0 attempt(s) (bound=3). Domain is judgment-only (no exact property to decide) -> ARMOR-ONLY; do NOT invent a fake gate. Recorded so the domain is not wastefully re-attempted.

- **Domain B prediction held: True** (route-armor=True, outcome-armor-only-kappa0=True)

## Domain C — under-specified (control)
- router verdict: **ABSTAIN-NEED-INPUT**
- pipeline outcome: **ABSTAIN_NEED_INPUT**

- **Domain C prediction held: True**

---
## OVERALL: predictions ALL HELD (A=True, B=True, C=True)

Honest note: a κ=1 verifier was earned by the RUN (Domain A cand 3), not by any self-report; the hallucinated and the soundness-incomplete candidates were REJECTED at the run step. The validated verifier carries only its known-answer coverage — toy-passing != frontier-correct.
