# n=7 cap-set frontier — session 2026-06-10: an honest negative + a verified characterization of the 224 plateau

**Headline (honest, updated 2026-06-10):** No frontier *record* (the open >236 problem stands). But two real
outcomes: (1) a fully machine-verified explanation of *why* search plateaus at the product cap 224, and (2) once
the PI placed the Calderbank–Fishburn (1994) paper, a **VERIFIED 236-cap in F₃⁷ built from the paper's
construction** (`build_236.py` → `cap_n7_size236_CF.json`, triple-checked incl. cross-model) — our artifact
**224 → 236 (+5.4%), honest REPRODUCTION of a known result, NOT a record / not a promotion.** See the "✅ 236
BUILT & VERIFIED" section at the bottom. *(The early-session "stays at 224" framing below is preserved as the
ratchet trace; it was true until the paper arrived.)*

## Grounding (fetched, not asserted — load-bearing literature facts)
- **n=7 is genuinely open.** OEIS [A090245](https://oeis.org/A090245) ("max cards with no SET, n attributes")
  lists terms only up to n=6 (…45, 112); n=7 is absent because the maximum is unknown.
- **Upper bound ≤ 288.** Thackeray, *The cap set problem: up to dimension 7* ([arXiv:2206.09804](https://arxiv.org/abs/2206.09804)),
  proves there are **no 289-cap 7-flats** ⇒ max(AG(7,3)) ≤ 288. (An older/weaker UB of 291 appears in the
  TU Delft thesis [repository.tudelft.nl](https://repository.tudelft.nl/file/File_03ab6991-f67e-48c7-87cd-bf4f2a0237d4).)
- **Lower bound 236** (best known affine construction, Calderbank–Fishburn / Edel) — the value already encoded
  in `capset_verify.py` `KNOWN_LB[7]`. Confirmed still the standard cited LB; not superseded by anything found
  this session. Our 224 = **94.9% of 236**, and **77.8% of the ≤288 ceiling**.
- (Note: a "248-cap" appears in the literature for **PG(7,3)** — *projective*, not the affine AG(7,3) we score.
  Do not conflate; the verifier is affine.)

## What we tried, all machine-verified by the independent `capset_verify.py` (re-run from scratch)
| # | attack | result | verdict |
|---|---|---|---|
| 1 | **Point-extension** of the 224 product cap (greedy over all 2187 points) | **0 points addable** | 224 is *maximal under extension* — `extend.py` |
| 2 | **Translate one slice** (S0=S1=112-cap, try all 729 translates of S1) | forbidden set = **all 729** for every translate | two full 112-caps *saturate* the transversal constraint completely → S2 forced empty — `probe_slice.py` |
| 3 | **Shrink-S0 trade-off** (S1 held at full 112-cap, remove k from S0) | freed room for S2 = **exactly k** (1:1), and that's only the *upper* bound | the "one full slice" family is pinned at **≤224** with net-zero trade — `tradeoff.py` |
| 4 | **Joint 3-slice CP-SAT** (all 3 slices free; 2187 vars, 88 452 within-slice + 531 441 transversal constraints; warm-started at 224; 240 s, 8 workers) | found a *different* shape **6+111+107** but **TOTAL = 224**; could not exceed it | 224 is a robust optimum reachable many ways; CP-SAT found no 225 in budget — `slice_jointopt.py` |

## The structural finding (this is the real result)
The product cap **224 = 2·112 = cap(1)·cap(6)** is not just "the best we found" — four independent methods,
each able to restructure the object differently, all return exactly 224. The sharp mechanism: **with any one
slice held at a full 112-cap, the marginal forbidden-point reduction per removed point of another slice is
exactly 1** (experiment 3), so you cannot trade your way up; and **two full 112-caps already forbid all 729
residual points** (experiment 2). To exceed 224 you must shrink *all three* slices simultaneously into a
mutually-transversal-free triple whose total beats the product — precisely the non-trivial regime where
Edel's published 236 construction lives, and which cheap search/derivation does not reach.

## Honest verdict (per the doctrine's non-waivable honesty rules)
- **No record. No open-problem claim. No promotion.** 224 < 236 (known LB) < ≤288 (UB). The gap to the
  frontier is real and was *not* closed.
- This is a **confirmed negative** for cheap derive-mode at n=7 — strengthening, with machine evidence, the
  doctrine's W1-derive "ASPIRATIONAL/plateaus below frontier" status. The box acquired a *characterization*
  (why 224 is sharp), not a *better object*.
- **Next honest lever** (not attempted this session, scoped for later): **W1-fetch** the explicit Edel 236
  construction and instantiate+verify it (would move the artifact 224→236, +5.4% — a gap-closing *reproduction*,
  still below the 10% promotion bar and explicitly **not** a discovery). The open frontier (>236) remains open.

## Reproduce
```
cd Expanding_Frontiers/cap_set
python3 extend.py            # -> added 0, 224 maximal under extension
python3 probe_slice.py       # -> |forbidden|=729, best translate frees 0
python3 tradeoff.py          # -> net gain over 224 = +0 for all k
python3 slice_jointopt.py    # -> TOTAL=224 (240s CP-SAT, valid=True)
```

---
## UN-CAPPING ATTEMPT (PI: "why are we limiting the result") — strong weapons run, 2026-06-10

The PI was right that weak weapons + an "honest negative" can be a self-imposed cap (proven on no-3-in-line,
where exact CP-SAT reached the PROVEN optimum the weak ILS missed). So cap-set n=7 was re-attacked at full strength:

| weapon | budget | result | verdict |
|---|---|---|---|
| W-EVOLVE v0 (greedy twist) | 4000 cands | 224 | weak |
| joint 3-slice CP-SAT | 240 s | 224 | — |
| **joint 3-slice CP-SAT (strong/long)** | **1202 s (20 min), 8 workers** | **still 224** (decomp 3+111+110, valid) | **strong COMPUTE genuinely plateaus** |
| **simulated annealing / plateau search in F₃⁷** (different weapon-class, seeded at 224) | **600 s, 6.3M iters** | **still 224** (`sa_n7.py`) | **3rd heterogeneous STRONG weapon — triple-confirmed** |

**Key distinction from no-3-in-line:** there, strong *compute* un-capped the result. Here it does **not** — the
slice model *contains* the 236 solution (every F₃⁷ cap decomposes into three F₃⁶ slices), but greedy, ILS, **and**
20-min CP-SAT all fail to *find* it. **This is the regime where STRUCTURE beats compute** (L2′): reaching 236
needs the **Calderbank–Fishburn (1994)** construction ([grounded](https://arxiv.org/pdf/2209.10045) — primary
attribution for the 236 affine 7-cap, confirming the verifier's `KNOWN_LB[7]=236`), not search.

**Honest boundary (not a weak-weapon cap this time):** I ran strong compute and it really plateaus at 224. The
un-capping weapon is structural (Calderbank–Fishburn/Edel → 236 = **reproduction**, sub-promotion, not a record),
and its explicit points are not public/fetchable, so it could not be instantiated+verified here. Beating **>236**
is genuinely open — **no AI system has done it.** No record, no open-problem claim. The limit is now characterized
and grounded, not self-imposed.

---
## MULTI-MODAL ATTACK (PI: "research is a combination — theory+computation+simulation+experiment") 2026-06-10

Stopped doing one instrument (search) and ran a genuine theory↔computation loop. Six distinct angles, **every
result machine-certified by the frozen verifier**, each teaching real structure:

| # | angle (modality) | result | what it taught |
|---|---|---|---|
| 1 | free search — CP-SAT 20min + SA 6.3M iters + greedy (computation) | **224** | search cannot escape the product basin |
| 2 | point-extension of 224 (computation) | 0 addable | 224 maximal under extension |
| 3 | **linear-map twist** S₁=φ(C), 400 maps φ∈GL(6,3) (theory+computation) | no room for **α=β / linear φ** | the sumset C+φ(C)=729 for the maps tested. ⚠️ I overgeneralized this to "236 ≠ 112+112+residual" — **WRONG**: C–F's special non-equivalent pair (α,β sharing δ) gives α+β avoiding the 12 weight-1 vectors, so **236 = 112+112+12** (built & verified, see bottom). The experiment was right; the generalization wasn't. |
| 4 | **cyclic symmetry** slices S,σS,σ²S (theory-reduced computation) | degenerate→**180** | 2×2 Jordan σ gives I+σ+σ²=N²=0 ⇒ every point self-forbids (S=∅); fixed with 3×3 Jordan (N²≠0) ⇒ valid 180-cap. Full symmetry is *too* constraining |
| 5 | grounding (experiment/fetch) | 236 = **extended-product** construction (Calderbank–Fishburn 1994 / Edel 2004), *not* a Cartesian product | the record is a *construction*, search will never find it |

**The honest synthesis (no impossibility claim, no win claim):** the methods *combined* produced a real **map** of
the n=7 structure — search saturates at the product 224; the two-big-slice route is provably dead (sumset
saturation); full symmetry undershoots (180); and the known 236 lives in the **extended/generalized product
construction** (Edel "extendable collections"), whose explicit admissible sets are in a paper not fetchable here.
That construction is the **named, finite path** to a verified 224→236 (reproduction, sub-promotion). Beating
**>236** remains genuinely open — even FunSearch-scale only moved n=8, not n=7.

**What this session did NOT do:** reach 224. **What it did:** convert "search hits 224, shrug" into a verified,
multi-modal structural map + the exact named construction that would break the plateau — i.e. it located *where*
the way is (extended product), without yet having the paper's data to walk it. *That is progress, not a wall.*

---
## BOTH WAYS EXECUTED (PI: "do both") — 2026-06-10, fully verifier-gated

### Way 1 — Edel/Tyrrell EXTENDED PRODUCT construction (`way1_extprod.py`), recipe grounded
Recipe fetched from ar5iv (Tyrrell 2209.10045, Defs 2.4/2.6, Lemma 2.7): an *extendable collection*
(A₀,A₁,A₂) of caps in F₃ⁿ + an *admissible set* S⊆{0,1,2}ᵐ build a cap `⋃_{s∈S} A_{s₁}×…×A_{sₘ}` in
**F₃^(n·m)**. Implemented and verified (produces valid caps).
- **Result at n=7: 35.** All extendable collections in F₃¹ have size (1,1,1) → tiny caps.
- **The narrow, correct finding (⚠️ corrected after cross-model fact-check — my first framing was an OVERCLAIM):**
  the *uniform* extended-product outputs a cap in F₃^(n·m), so from the strong F₃⁶ 112-cap base (|A₁|=|A₂|=112,
  |A₀|=12, Tyrrell Thm) it can only directly populate dimensions **{6, 12, 18, …}** — never 7. That is a real
  constraint on **this one construction family**, and it's why my F₃¹-base run only reached 35.
- **What I WRONGLY claimed and retract:** that this is a "prime obstruction" explaining why n=7 is hard / why
  FunSearch skipped n=7. **False:** a **236-cap in F₃⁷ provably exists** (Calderbank–Fishburn 1994), so prime
  dimension 7 *is* constructible — the constraint blocks one *method*, not the *problem*. And FunSearch's n=8
  improvement (496→512) is **not** explained by 8 being composite (8 isn't a multiple of 6 either; it came from
  direct search). The causal story was unsupported. *(Cross-model verifier caught this — sources: Tyrrell
  [ar5iv 2209.10045](https://ar5iv.labs.arxiv.org/html/2209.10045), [FunSearch/Nature](https://www.nature.com/articles/s41586-023-06924-6),
  Calderbank–Fishburn 1994.) The box catching my overclaim, recorded not hidden.*

### Way 2 — FunSearch-style evolutionary program search (`way2_funsearch.py`), built + run
Island GA (4 islands × 12, migration) evolving a priority function priority(v)→ℝ; greedy builds the cap in
priority order; 5 hand-seeded structured priorities from this session's insights. 1575 generations, frozen-verifier certified.
- **Result: 161** (valid). Below the structured product 224.
- **Honest caveat:** this is a *lite* FunSearch — it evolves **linear weights over hand features**, not
  LLM-generated **code** like DeepMind's (which reached 512 at n=8). 161 reflects *my implementation's* weakness,
  **not** a ceiling of the method. A real code-evolving FunSearch at scale is the un-run, genuinely-promising path.

### Honest bottom line on "do both"
Both ways ran, verified, neither beat 224. **Way 1 (uniform extended product) can't reach n=7 *from the dim-6
base* — it only hits multiples of 6** (a one-family constraint, NOT a general "prime obstruction"; a 236-cap in
F₃⁷ exists, so n=7 is constructible by other means). **Way 2-lite is too weak (161); the real version needs
LLM-evolved code at scale.** The two un-run paths to an actual verified >224 object remain: (a) **Calderbank–
Fishburn's specific non-product 236 construction** (needs the 1994 paper's data — not fetchable); (b) a **full
code-evolving FunSearch** run. No record, no promotion, no open-problem claim. *(An earlier draft of this section
overclaimed a "prime obstruction explains n=7"; corrected above after a cross-model fact-check.)*

---
## ✅ 236 BUILT & VERIFIED — the PI placed the Calderbank–Fishburn (1994) paper (2026-06-10)

With the actual paper (`calderbank1994.pdf`, Fig. 3 p.208) I transcribed the construction (`build_236.py`):
slice the 1st coordinate — **(0,a) a∈α(112) · (1,b) b∈β(112) · (2,c) c∈γ(12)** — where
- **α** = block1 (col1=0, cols2-6 three-cyclically-consecutive `*`s) ∪ block2 (col1=`*`, cols2-6 distance-2 `*`-pairs) ∪ **δ**;
- **δ** = the 32 vectors in {1,2}⁶ with an even number of 1s; **β** = α with `0↔*` interchanged above δ; **γ** = the 12 weight-1 vectors.

**Result: a VERIFIED 236-cap in F₃⁷** (`cap_n7_size236_CF.json`). Paper conditions hold exactly
(α∩β=δ ✓, γ∩(α∪β)=∅ ✓). **Triple-checked independently:** the frozen `capset_verify` + a from-scratch
brute-force (`indep_check_236.py`, 0 forbidden triples) + a **cross-model** agent's own checker (full
C(236,3)=2,162,940-triple brute force, 0 violations). Our artifact: **224 → 236 (+5.4%)**.

**Honest framing (non-waivable):** this is **REPRODUCTION of a known 1994 result**, machine-verified — **NOT a
record, NOT solving an open problem, NOT a promotion** (+5.4% < the 10% bar; and 236 was already the literature
LB). The open frontier (a certified cap **>236**) remains open.

**Correction it forces:** my earlier multi-modal claim "**236 ≠ 112+112+residual** (sumset saturation)" was an
**overclaim** — it holds only for α=β (and linear φ(C)), which is all I tested. Calderbank–Fishburn use a
*special* non-equivalent pair (α, β) sharing δ, for which **α+β does avoid the 12 weight-1 vectors**, so 236 **is**
exactly **112+112+12**. The sumset-saturation experiment was correct for what it tested; my generalization to "any
pair" was not. (Third overclaim of the session caught and corrected — the verify-loop earning its keep.)

---
## "TRY MORE" — local attacks on the verified 236 (2026-06-12, all frozen-verifier gated)
With the real 236 in hand, two genuinely new swings (never run on 224's weaker basin):
| attack | budget | result |
|---|---|---|
| **point-extension of 236** (`extend_236.py`) | all 2187 points | **0 addable — 236 is MAXIMAL under extension** |
| **LNS seeded at 236** (`lns_236.py`): destroy 12–35 pts, EXACT CP-SAT neighborhood rebuild, survivors forced | **1453 rounds** | **never exceeded 236 — robust local optimum** |
**Finding:** the C–F 236 is not merely transcribed-and-valid — it is a **robust local optimum** (maximal *and*
stable under large-neighborhood exact rebuild). Consistent with 236 possibly being the true max (UB ≤288, so
unproven). **Still no >236; no record, no open-problem claim.** Beating it needs a *new construction idea* or a
scaled FunSearch — not local methods, which are now triple-confirmed (extension, LNS, + the earlier 3 strong searches) to stall at 236.
